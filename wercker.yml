box: alpine:3.6

build:
  steps:
    - script:
      name: Install Bats
      code: |
        apk update
        apk add bats

    - internal/watch:
      code: bash bin/tests
      reload: true

    - script:
      name: Run Tests
      code: |
        bash bin/tests
